
def bubble_sort(nums: list[int]) -> list[int]:
    """
    swap the first num with each num in the list if it's larger than that num.
    stop when there's been no swaps while iterating.

    O(n^2)
    """
    ...
